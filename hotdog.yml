# Example hotdog configuration
#
#
---
global:
  listen:
    address: '127.0.0.1'
    port: 1514
    tls: false

# Fields upon which rules can apply:
#   - severity
#   - facility
#   - timestamp
#   - hostname
#   - appname
#   - msg
rules:
  - field: msg
    regex: '^hello\s+(?P<name>\w+)?'
    action: forward
